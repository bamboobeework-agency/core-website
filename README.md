This repository contains the react based code of Bamboobee's website.

## To Run Locally

**Prerequisites:**  Node.js


1. Clone the repo locally.
2. Install dependencies:
   `npm install`
3. Run the app:
   `npm run dev`
